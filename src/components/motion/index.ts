/**
 * Public API of `src/components/motion/` — scroll-triggered and
 * other higher-level motion building blocks built on framer-motion.
 */
export { RevealOnScroll, RevealItem } from './RevealOnScroll'
export type {
  RevealFallback,
  RevealOnScrollProps,
  RevealItemProps,
} from './RevealOnScroll'
